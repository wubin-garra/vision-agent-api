services:
  - type: web
    name: vision-agent-api
    runtime: node
    plan: free
    region: singapore
    # Render 构建阶段常带 NODE_ENV=production，npm 会跳过 devDependencies；
    # typescript / @types/* 在 devDependencies，必须 --include=dev 才能 tsc。
    buildCommand: npm ci --include=dev && npm run build
    startCommand: node dist/index.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: DEBUG
        value: "false"
      - key: NODE_ENV
        value: production
      - key: LLM_PROVIDER
        value: deepseek
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: DEEPSEEK_LLM_MODEL
        value: deepseek-v4-pro
      - key: DEEPSEEK_ROUTER_MODEL
        value: deepseek-v4-flash
      - key: VISION_PROVIDER
        value: openai
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_BASE_URL
        value: https://api.openai.com/v1
      - key: OPENAI_VISION_MODEL
        value: gpt-4o-mini
      - key: DATABASE_PATH
        value: /tmp/vision_agent.db
      - key: UPLOAD_DIR
        value: /tmp/uploads
      - key: JWT_SECRET
        generateValue: true
